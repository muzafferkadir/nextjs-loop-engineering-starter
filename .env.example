# SQLite database file. The default works out of the box — no env setup
# is required to run this starter. Copy this file to .env only if you
# want to override the location (e.g. file:./db/test.db in CI).
DATABASE_URL=file:./db/dev.db
